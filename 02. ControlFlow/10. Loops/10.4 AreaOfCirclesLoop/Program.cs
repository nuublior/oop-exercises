const double pi = 3.14159;

for (double r = 1; r <= 5; r += 2)
{
    double area = pi * r * r;  // Calculate the area of the circle
    Console.WriteLine("For radius r = " + r + ", the area is " + area);
}
